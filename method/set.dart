void main() {
  // Set a = {10, 20, 30};
//  print(a.iterator);

  // Set a = {10, 20,30};
  // print(a.length);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.first);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.isEmpty);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.isNotEmpty);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.last);

  // Set a = {20};
  // print(a.single);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.hashCode);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.runtimeType);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.add(55));

  // Set a = {10, 20,50,60,70 };
  // a.addAll({5, 20, 30, 40});
  // print(a);

  // Set a = {10, 20, 30, 40, 50};
// print(a.any((element) => element <=2));

  // Set a = {10, 20, 30, 40, 50};
  // print(a.cast());

  // Set a = {10, 20, 30, 40, 50};
  // a.clear();
  // print(a);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.contains(10));

  // Set a = {10, 20, 30, 40, 50};
  // print(a.containsAll({10, 20, 60}));

  // Set a = {10, 20, 30, 40, 50};
  // print(a.difference({10, 20, 60, 70, 40}));

  // Set a = {10, 20, 30, 40, 50};
  // print(a.elementAt(3));

  // Set a = {10, 20, 30, 40, 50};
  // final b = a.every((key) => key <5);
  // print(b);

  // Set a = {10, 20, 30, 40, 50};
  // print(a.expand((c) {
  // print(a.expand((c) {
  //   for (var i = 0; i < 2; i++) {
  //    int a=i;
  //     break;
  //     }
  //   return a;
  // }));









  Set a = {10, 20};

  // print(a.iterator);

  // print(a.length);

  // print(a.first);

  // print(a.isEmpty);

  // print(a.isNotEmpty);

  // print(a.last);


  // Set b={10};
  // print(b.single);

  // print(a.hashCode);

  // print(a.runtimeType);

  // print(a.add({50, 10}));

  // print(a.any((abc) => abc >= 5));

  // print(a.cast());

  // print(a.contains(10));

  // print(a.containsAll({10, 20, 30}));

  // print(a.difference({10}));

  // print(a.elementAt(0));

  // Set a = {10, 20, 30, 40, 50};
  // print(a.every((element) => element <= 20));

  // print(a.expand((element) => a));

  // print(a.firstWhere((element) => false));
  
  // final b = 100;
  // final re = a.fold(b, (c, element) => b + element);
  // print(re);

  // print(a.followedBy(a));
  // Set b = {10, 20};

  // final data = a.intersection(b);
  // print(data);

  // print(a.join());

  // print(a.lastWhere((element) => false));


  // print(a.reduce((c, d) => c + d));

  // print(a.remove(20));

  // print(a.singleWhere((element) => false));

  // print(a.skip(0));

  // print(a.skipWhile((value) => false));

  // print(a.toList());

  // print(a.toSet());

  // print(a.toString());

  // print(a.union(a));

  // print(a.where((element) => false));
  // print(a.whereType());
}
