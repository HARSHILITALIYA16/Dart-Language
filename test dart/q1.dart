void main() {
  // final num = [1, 2, 3, 8];
  // final result =
  //     num.fold<int>(0, (previousValue, element) => previousValue + element);
  // print(result);

  // final result =
  //     num.fold(b, (previousValue, element) => previousValue + element);

  // String num = "12347";
  // final a = num.split('');
  // print(a);
  // int b = 0;
  // a.forEach((element) {
  //   b = b + int.parse(element);
  // });
  // print(b);
}
